# 地圖標註系統 (Map Annotation System)

這是一個基於 Web 技術的地圖標註系統，支援即時定位、標註管理、路徑追蹤與雲端同步功能。

## 功能特色

- 🗺️ **地圖顯示**：整合 OpenStreetMap
- 📍 **標註管理**：新增、編輯、刪除、分組標註點
- ☁️ **雲端同步**：支援 Supabase 登入與資料同步
- 📱 **PWA 支援**：可安裝為應用程式，支援離線使用
- 🔔 **位置提醒**：接近標註點時自動發送通知
- 🛤️ **路徑追蹤**：記錄移動路徑

## 如何在 GitHub Pages 上運行

1. 將本專案的所有檔案上傳至您的 GitHub Repository。
2. 進入 Repository 的 **Settings** > **Pages**。
3. 在 **Build and deployment** 下，選擇 **Source** 為 `Deploy from a branch`。
4. 選擇 **Branch** 為 `main` (或 `master`) 並選擇資料夾為 `/ (root)`。
5. 點擊 **Save**。
6. 等待部署完成後，GitHub 會提供您的網站網址 (例如 `https://your-username.github.io/repo-name/`)。

## Supabase 設定注意事項

若您部署到 GitHub Pages，請務必在 Supabase 後台更新您的專案設定：

1. 進入 Supabase Dashboard > Authentication > URL Configuration。
2. 將 **Site URL** 修改為您的 GitHub Pages 網址。
3. 確保 **Redirect URLs** 包含您的 GitHub Pages 網址。

## 本地開發

若要在本地運行，請確保使用 Web Server (如 VS Code 的 Live Server 插件)，而不是直接雙擊打開 `index.html`，以確保 Service Worker 和模組功能正常運作。
